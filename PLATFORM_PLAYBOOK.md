# 平台报表流水线 · 经验手册

**写给谁：** 要给新平台（Falabella CL / TikTok Shop MX / Walmart MX …）做同类
流水线的人，以及协助他做这件事的 Claude 会话。

**这份文档不讲 MercadoLibre 的账务规则。** 那部分不可移植 —— 每个平台的费用
结构、税务处理、退款机制都不一样，照搬只会把错误的假设带过去。这里只写**换个
平台仍然成立**的东西：架构、采集层的硬约束、踩过的坑、以及哪些数字必须实测。

每一条都对应一次真实的代价。没付过代价的"最佳实践"不在这里。

---

## 0. 这套东西叫什么

没有一个词能盖全，分三段看：

| 阶段 | 行业叫法 |
|---|---|
| 驱动浏览器把报表点下来（因为没有 API） | **RPA**（Robotic Process Automation），或 portal / screen scraping |
| xlsx/csv → 清洗 → Parquet / DuckDB | **ETL**，这一段是标准 ETL |
| 平台结算单 × 订单明细对账 → 财务报表 | **marketplace payout reconciliation** / 财务结账自动化 |

整体可以叫 **marketplace financial data pipeline**。对财务说"卖家回款对账
自动化"，对工程说"RPA 采集 + ETL + 对账引擎"。

**别只说 ETL。** ETL 只覆盖中间那段，而中间那段恰恰是最简单的。真正难的是
两头：在一个随时会变、没有契约的网页上稳定拿到文件，以及把平台的钱和自己的
订单对上。说成 ETL 会让人以为这是个已被商品化的问题。

---

## 1. 分层：四层，跨进程

```
采集（浏览器）──► 清洗（pandas/duckdb）──► 报表（openpyxl）──► 调度/界面
   会失败、慢         会崩、纯本地          纯计算、可重跑      无人值守
```

**四层之间用子进程而不是 import。** 采集要驱动浏览器跑几小时，清洗要吃 pandas。
任何一边崩了都不该把另一边连坐 —— 一次 pandas 异常不该毁掉刚跑完三小时的
下载批次。子进程最坏只是返回一个非零退出码。

**下载和出报表必须能分开跑。** 下载是慢的、依赖网络和浏览器的、会失败的；
出报表是快的、纯本地的、可以随便重跑的。改报表逻辑时不该重新下载一遍。
这条在开发期能省掉大量时间。

唯一值得的例外：**把报表层的 import 提前做一次**。出报表排在四十分钟的下载
之后，等跑到那步才发现 pandas 装坏了太晚 —— 在下载之前就 import 一次，
失败就立刻退出，那时候什么都还没做。

---

## 2. 采集层：紫鸟的机制与硬约束

新平台大概率仍然用紫鸟（同一个企业账号、同一套代理），所以这一节可以直接复用。

**连接方式是"附着"不是"启动"。** 紫鸟客户端以 `--run_type=web_driver --ipc_type=http`
启动后监听一个本地端口；`startBrowser` 返回 `debuggingPort`，Selenium 用
`debuggerAddress` 附着上去。**永远不要 `driver.quit()`** —— 那会把附着的浏览器
一起杀掉。关店要走 `stopBrowser`。

**每家店的内核自带 `webdriver.exe`。** 没跑过紫鸟的机器第一次开店要先下几百 MB
的内核，`webdriver.exe` 就在里面。给足等待（实测留 600 秒），并且判断"下完了"
要看**文件大小连续若干秒不变**，不能看文件存在 —— 下到一半的文件也存在。

**单实例锁。** 客户端普通模式在跑时，第二个实例抢不到端口。要切 webdriver 模式
必须先让它完全退出（`ziniao.exe` 主进程），而不只是关掉店铺窗口。

**Session 0 是绝对的。** 紫鸟是带界面的 Electron 程序。Windows 计划任务的
「不管用户是否登录都要运行」在 session 0 里没有交互桌面，浏览器起不来。
定时任务**必须**用「只在用户登录时运行」（`schtasks /IT`），机器要自动登录、
不睡眠、不锁屏。这条改变部署方案的选型，越早知道越好。

**下载目录必须传绝对路径。** CDP 的 `Browser.setDownloadBehavior` 把相对路径
解析成浏览器进程自己的工作目录，文件会消失在紫鸟安装目录某处，而每条路线都
报告"拿到 0 个文件"，看起来像点击失败。

**判断"下载完成"靠目录安静下来。** 平台的"全选并下载"会隔几秒触发多个下载，
没有任何信号说"这是最后一个"。做法：忽略 `.crdownload`/`.tmp`，等目录连续
N 秒没有新文件。而且**预期文件数是预测不是承诺** —— 平台只提供有数据的报表
类型，少于预期时要有退出机制，不能死等到超时。

**慢报表要能异步取。** 有些报表（MercadoPago 的 `Liberaciones`）要几十分钟到
几小时。正确做法是分两阶段：先把所有慢报表**申请**掉（它们在服务端排队），
再去做别的实事，最后回来收。收不到的记进一个待取清单，下次再收。

> 待取清单存的必须是**报表的业务标识**（类型 + 期间），不是浏览器句柄。
> 这样关店、重启、甚至重启电脑都不影响补取。

---

## 3. 定位元素的纪律

这一节是整个采集层最容易埋雷的地方。

**优先级：`data-*` 属性 > 结构选择器 > 可见文字。**

MercadoLibre 那套代码里有几十处按西语文字定位（`Ir al detalle`、`descargar`、
`almacenamiento`、`/ultimos?\s*(\d+)\s*mes/`），导出文件的列名也是西语 ——
而**导出文件的语言跟着账号的界面语言走**。有人在浏览器里把语言改成 English，
后果分两种：

* 元素找不到 → 抛错，看得见，可以修
* 页面上的月份名核对失败 → **整个账期被静默跳过**，报表少算费用而毫无提示

实测：漏掉一期账单让某店少算佣金 973.23，代扣税率从 9.172% 虚高到 9.221%。
**第二种才是真正的危险**，因为它长得像正常结果。

补救方式是加一道语言关卡，而关卡的判据必须是**语言无关的**：

```js
document.querySelector('.nav-user-menu-language-switcher')
        .getAttribute('data-selected-locale')   // "es_MX"，不是「Idioma Español」
```

用可见文字判断语言，语言一变判断自己就先瞎了。

**给新平台的规矩：** 写第一个选择器的时候就去翻 DOM 找 `data-*`、`role`、
`aria-*`、稳定的 id。找不到再用文字，并且立刻把"语言/文案变了会怎样"写进注释。

---

## 4. 健壮性的铁律

**一个失败单元不能拖垮整批。** 一家店的坏文件不该毁掉其他 11 家的汇总表；
一条路线的选择器失效不该毁掉另外三条。把每个单元包在守卫里，记录错误、继续
往下跑，最后在汇总里摆出来。**隔离不等于隐藏** —— 必须体现在汇总和退出码里。

**退出码要分档，"部分失败"不等于"失败"。**

```
0  全部成功
1  部分失败 / 有需要关注的项，但产出已经写出来了
2+ 真失败，没有可用产出
```

55 个文件里坏 1 个报成"清洗失败"，会让人以为整批数据都没洗，去查错的地方。
而且**未捕获的异常不能借用 1 这个码** —— Python 默认就退 1，会把一次彻底崩溃
伪装成"部分失败"。顶层兜住异常并退 2。

**白名单失败即拒绝。** 配置文件读不出来时，绝不能退化成"不限制"。一个漏掉的
逗号让脚本可以打开账号下全部 93 家店，这不是小事。解析失败就报错退出。

**幂等和可重入。** 同一个月重跑不该产生副作用。加锁防重入，而锁要**记 PID
并检查进程死活** —— 只看文件年龄不够：跑批中途被强杀时锁会留下，而下一次
定时运行往往就在几分钟后。实测过 10:01 被杀、10:10 的定时任务被锁挡掉。

> Windows 上**不能用 `os.kill(pid, 0)` 探测进程**：CPython 的 `os.kill` 在
> Windows 上走 `TerminateProcess`，信号 0 会把目标进程**杀掉**。用 `tasklist`。

**永不覆盖人手工填的东西。** 报表里如果有让人填的列（采购成本之类），重新
生成会把它清零。同一个月反复出报表就是在反复清空财务的工作。要么写到新文件，
要么读回旧文件里的值。

---

## 5. Windows 上的坑

| 坑 | 症状 | 做法 |
|---|---|---|
| 控制台编码 | 管道/重定向时 `UnicodeEncodeError`，一句 print 毁掉整批 | 入口 import 一个模块强制 stdout/stderr 为 UTF-8，子进程传 `PYTHONUTF8=1` |
| 控制台程序的输出 | 中文机器上读 `schtasks` 等的输出是乱码 | 它们写的是 **OEM 代码页**，按 `oem` 解码而不是 utf-8。英文系统 OEM 是 437（纯 ASCII），所以这个 bug 在英文机器上测不出来 |
| `.cmd` 行尾 | `if ( … )` 块在部分 Windows 上解析异常 | 批处理必须 CRLF。`.gitattributes` 里 `* text=auto eol=lf` 会把它转成 LF，要加 `*.cmd text eol=crlf` |
| `.cmd` 里的中文 | 双击运行时乱码 | 文件存 UTF-8 无 BOM + 开头 `chcp 65001`，关键命令行保持 ASCII |
| `.gitignore` 行内注释 | 规则静默失效，敏感文件被提交 | `#` 只有在**行首**才是注释。写完每条用 `git check-ignore -v` 验一遍 |
| 父目录被忽略 | 子目录 `!` 取反不生效 | 要写成 `dir/*` + `!dir/sub/` |
| 计划任务 | 三项关键设置命令行没有开关 | 生成完整任务 XML 再 `schtasks /create /xml` 导入（XML 必须 UTF-16） |

---

## 6. 数据与口径

这一节是账务设计的通则，具体规则每个平台都不同。

**先分清两个时间基准。** 收入按**订单日**归集，平台费用按**计费日**入账，两者
不重合：月末几天的订单，佣金常记在下一期账单上。做月报必须明确每个科目用哪个
基准，并且**做一张两种口径的对比表** —— 差额说不清就是有科目没识别。

推论：**只下目标月的账单一定少算费用**。要连下之后 1–2 期。

**派生字段必须标成派生。** 有些数字平台不给，是算出来的（例如"代扣税 = 含税
佣金 − 纯佣金"）。这类字段要在报表里写明来源，否则三个月后没人说得清它是
平台给的还是脚本算的。

**校验就是产品本身。** 一套报表最值钱的不是数字，是"这些数字自洽"这件事被
证明过。做法：

* 逐笔勾稽（明细加总 = 表头总额），给出残差和阈值
* 多张桥表把不同口径连起来，每座桥都必须能平
* 每条校验都写清楚"不通过意味着什么"，而不只是红叉
* **跑完只显示一个绿色"完成"等于把校验全扔了** —— 未通过的项要显式摆出来

**同一个数字不要在三个地方各算一遍。** 一处改了另两处没改，就会出现"报表自己
说等于，实际差一个调整额"这种最难查的问题。算一次，传下去。

**空值和类型。** pandas 2 → 3 的 `astype(str)` 对 NaN 行为不同（2.x 给 `'nan'`
字符串，3.x 保留 NaN）。任何"把列转成字符串再按字面量过滤"的代码都会在升级后
静默失效。转字符串前先 `fillna("")`，或者别依赖字面量。

**时区。** TikTok 的导出文件名带 `(UTC-6)`。平台的"日"和你的会计"日"未必是
同一个 —— 跨月的几笔会落到不同月份。开工第一天就把每个平台的时区口径写下来。

---

## 7. 运维

**告警只在真出事时发。** 如果某条校验每个月每家店都不过（已知的口径问题），
就不要为它告警 —— 财务每月都看到一个"报表异常"，两个月后这个文件彻底没人看。
会触发告警的只该有两类：**数据没拿到**、**产出没生成**。

**告警要放在人一定会看到的地方。** 计划任务会把 stdout 丢掉，没人会主动翻
日志目录。写一个文件到财务每月都会打开的那个目录里。

**日志每行都 flush。** 计划任务超时会强杀进程，缓冲区里的内容全丢 —— 而那
恰恰是最需要看的部分。

**给不写代码的人一个界面。** 一个 tkinter 小窗口（子线程读子进程 stdout、
队列传给主线程渲染、Tk 控件只在主线程碰）就够了。界面只负责拼命令行和贴输出，
**一行业务判断都不要放进去**：界面坏了不该影响出数，出数的口径也不该藏在
界面里。

**同一个机器人账号不要多机共用。** 防关联浏览器的核心承诺是"一家店 = 一个
指纹 + 一个代理"，两台机器同时开同一家店必然破坏它；同一个子账号多处登录也
可能被单点登录互踢，把跑到一半的批次打断。

---

## 8. 上线前必须实测的数字

别猜，测。这些数字决定部署方案：

* **单店一轮耗时**，以及 N 家店串行的总时长（MX：单店约 15 分钟，12 家
  2h48m–4h00m，实测三次）
* **单次跑批占多少磁盘**（MX：12 家 23 MB）→ 决定保留策略
* **每种报表从申请到可下载要多久** → 决定要不要异步两阶段
* **哪些店/哪些报表没有权限** → 这是账号问题，代码绕不过去
* **跨账期的费用溢出有多大** → 决定要连下几期账单

---

## 9. 不要照搬的东西

* **账务规则**。费用科目表、税率推导、退款/取消的处理、SKU 归集 —— 全是
  平台特有的。照搬等于把错误假设带进新平台。
* **列名常量**。每个平台的导出列名完全不同，而且跟着界面语言变。
* **报表的十张子表**。可以借鉴**形状**（从结论到证据再到缺口、桥表必须平、
  最后一页列出还欠什么），不要借鉴内容。

可以直接复制的：紫鸟客户端封装、白名单、批次编排与失败隔离、待取清单、
调度壳、两个 GUI、编码保护。这些在 MX 项目里对平台名的引用接近于零。

---

## 10. 新平台的第一周

1. **先手工走一遍**，把每张报表在页面上点出来，记下：入口路径、有哪些筛选项、
   生成是同步还是异步、文件名长什么样、表头在第几行。（三个新平台的样本已经在
   `D:\Projects\platform_data_samples`，先读文件比先写代码有用。）
2. **写探路脚本，不写生产代码**：开店 → 跳转 → 转储页面（截图 + html +
   可见元素清单）→ 人看 → 再写下一步。DOM 一无所知的时候，靠转储迭代比靠
   猜选择器快一个数量级。
3. **一条路线先跑通到底**（能稳定拿到文件），再铺开到其他路线。
4. **第一个完整批次跑完就去量第 8 节那些数字。**
5. **账务规则最后做**，而且要先在 Excel 里人工对一遍，确认口径对了再写代码。
   代码写的是已经确认的规则，不是用来探索规则的。
